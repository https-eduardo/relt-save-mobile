import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  optionMultiSelectContainer: {
    gap: 8,
  },
  optionsRow: {
    flexDirection: "row",
    gap: 8,
  },
  label: {
    fontSize: 16,
    fontFamily: "InterMedium",
  },
});
